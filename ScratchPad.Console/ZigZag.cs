namespace ZigZag;

public class Solution {
    public string Convert(string s, int numRows) {
        return "11" + Environment.NewLine + "2 ";
        return string.Empty;
        // use 2d array of chars
        // rows = param
        // columns = ? row - (s.Length)
        // zigzaglength = ?
    }
}